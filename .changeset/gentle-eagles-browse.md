---
"@browserbasehq/eve": minor
---

Publish the Browserbase Eve extension with Stagehand V4 Code Mode's `run`, `snapshot`, and
`screenshot` browser automation surface. This replaces the package's previous Browserbase-specific
search, fetch, session, navigation, action, and extraction tools; Eve's built-in web tools are
unchanged.
