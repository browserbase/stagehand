---
"@browserbasehq/eve": minor
---

Move the provider-official Eve extension into the Stagehand repository and replace the focused
Browserbase tools with Stagehand V4 Code Mode's `run`, `snapshot`, and `screenshot` surface. The
legacy `search`, `fetch`, session, navigation, action, and extraction tools are removed.
