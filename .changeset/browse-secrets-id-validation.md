---
"browse": patch
---

Reject malformed cloud resource IDs before making API requests, preventing empty IDs and reserved route names from selecting unrelated endpoints. Share UUID validation across projects, sessions, contexts, extensions, secrets, and functions while preserving saved context names.
