---
"@browserbasehq/stagehand-extension": patch
"@browserbasehq/stagehand-go": patch
"@browserbasehq/stagehand": patch
"@browserbasehq/stagehand-python": patch
---

experimental Jev path: opt-in `extract` modes — `"judge"` replaces the completion LLM call, `"pick"` lets Jev choose the elements whose text is copied into the schema
