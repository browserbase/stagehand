---
"@browserbasehq/stagehand-integrations-cursor-sdk": patch
"@browserbasehq/stagehand-integrations-example-cursor-facade": patch
---

Use the shared Cursor SDK session in the documented facade example, including settings isolation, event consumption, and cancellation cleanup. Allow hosts to supply an explicit Cursor API key without falling back to an unrelated environment.
