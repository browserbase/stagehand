---
"browse": minor
---

Add project secret CRUD and public-key commands, plus function secret attachment,
detachment, and listing. Create and update encrypt values locally with the
project's HPKE public key; values can be entered through a hidden prompt or stdin.
