---
"browse": patch
---

Align `browse functions` with the Browserbase Functions SDK by forwarding explicit project IDs during local development and publishing, matching local invocation context and error payloads, and accepting the SDK's `--api-url` option.
