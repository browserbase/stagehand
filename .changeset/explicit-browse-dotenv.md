---
"browse": minor
---

Disable deprecated implicit `.env` loading. Export credentials in the caller's environment or set `BROWSE_LOAD_DOTENV=1` to explicitly retain `.env` loading; existing environment variables still take precedence.
