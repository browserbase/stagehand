---
title: "page"
description: "Navigate, inspect, and interact with a browser page."
icon: "page"
---

A `Page` combines deterministic browser controls with Stagehand's natural-language `act`, `observe`, and `extract` methods.

<View title="TypeScript" icon="js">

## Quick start

```typescript
const page = await stagehand.context.newPage();
await page.goto("https://example.com");
```

## goto()

Navigate the page to a URL.

```typescript
await page.goto("https://example.com");
```

<ParamField path="url" type="string">
  The destination URL.
</ParamField>

<ParamField path="options" type="PageNavigationOptions" optional>
  Options that configure this operation.

<ParamField path="options.timeout" type="number" optional>
  Maximum wait time in milliseconds.
</ParamField>

  <ParamField path="options.waitUntil" type="LoadState" optional>
    The target load state.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<Page>">
  The operation result.
</ResponseField>

## reload()

Reload the current page.

```typescript
await page.reload();
```

<ParamField path="options" type="PageReloadOptions" optional>
  Options that configure this operation.

<ParamField path="options.ignoreCache" type="boolean" optional>
  Whether to bypass the browser cache.
</ParamField>

<ParamField path="options.timeout" type="number" optional>
  Maximum wait time in milliseconds.
</ParamField>

  <ParamField path="options.waitUntil" type="LoadState" optional>
    The target load state.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<Page>">
  The operation result.
</ResponseField>

## goBack()

Navigate backward in the page history.

```typescript
await page.goBack();
```

<ParamField path="options" type="PageNavigationOptions" optional>
  Options that configure this operation.

<ParamField path="options.timeout" type="number" optional>
  Maximum wait time in milliseconds.
</ParamField>

  <ParamField path="options.waitUntil" type="LoadState" optional>
    The target load state.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<Page>">
  The operation result.
</ResponseField>

## goForward()

Navigate forward in the page history.

```typescript
await page.goForward();
```

<ParamField path="options" type="PageNavigationOptions" optional>
  Options that configure this operation.

<ParamField path="options.timeout" type="number" optional>
  Maximum wait time in milliseconds.
</ParamField>

  <ParamField path="options.waitUntil" type="LoadState" optional>
    The target load state.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<Page>">
  The operation result.
</ResponseField>

## click()

Click at page coordinates.

```typescript
const xpath = await page.click(240, 320);
```

<ParamField path="x" type="number">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="number">
  The vertical page coordinate.
</ParamField>

<ParamField path="options" type="PageClickOptions" optional>
  Options that configure this operation.

<ParamField path="options.button" type="MouseButton" optional>
  The mouse button to use.
</ParamField>

<ParamField path="options.clickCount" type="number" optional>
  The number of clicks to dispatch.
</ParamField>

  <ParamField path="options.returnXpath" type="boolean" optional>
    Whether to return the affected element's XPath.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<string>">
  The operation result.
</ResponseField>

## hover()

Move the pointer to page coordinates.

```typescript
const xpath = await page.hover(240, 320);
```

<ParamField path="x" type="number">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="number">
  The vertical page coordinate.
</ParamField>

<ParamField path="options" type="PageHoverOptions" optional>
  Options that configure this operation.

  <ParamField path="options.returnXpath" type="boolean" optional>
    Whether to return the affected element's XPath.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<string>">
  The operation result.
</ResponseField>

## scroll()

Scroll from page coordinates by the supplied deltas.

```typescript
const xpath = await page.scroll(240, 320, 0, 600);
```

<ParamField path="x" type="number">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="number">
  The vertical page coordinate.
</ParamField>

<ParamField path="deltaX" type="number">
  Horizontal scroll distance.
</ParamField>

<ParamField path="deltaY" type="number">
  Vertical scroll distance.
</ParamField>

<ParamField path="options" type="PageScrollOptions" optional>
  Options that configure this operation.

  <ParamField path="options.returnXpath" type="boolean" optional>
    Whether to return the affected element's XPath.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<string>">
  The operation result.
</ResponseField>

## dragAndDrop()

Drag from one page coordinate to another.

```typescript
const [from, to] = await page.dragAndDrop(100, 100, 400, 400);
```

<ParamField path="fromX" type="number">
  The drag origin's horizontal coordinate.
</ParamField>

<ParamField path="fromY" type="number">
  The drag origin's vertical coordinate.
</ParamField>

<ParamField path="toX" type="number">
  The drop target's horizontal coordinate.
</ParamField>

<ParamField path="toY" type="number">
  The drop target's vertical coordinate.
</ParamField>

<ParamField path="options" type="PageDragAndDropOptions" optional>
  Options that configure this operation.

<ParamField path="options.button" type="MouseButton" optional>
  The mouse button to use.
</ParamField>

<ParamField path="options.delay" type="number" optional>
  The input delay in milliseconds.
</ParamField>

<ParamField path="options.returnXpath" type="boolean" optional>
  Whether to return the affected element's XPath.
</ParamField>

  <ParamField path="options.steps" type="number" optional>
    The number of intermediate pointer moves.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<[string, string]>">
  The operation result.
</ResponseField>

## type()

Type text at the current input target.

```typescript
await page.type("Browserbase");
```

<ParamField path="text" type="string">
  The text to type.
</ParamField>

<ParamField path="options" type="PageTypeOptions" optional>
  Options that configure this operation.

<ParamField path="options.delay" type="number" optional>
  The input delay in milliseconds.
</ParamField>

  <ParamField path="options.withMistakes" type="boolean" optional>
    Whether to simulate occasional typing mistakes.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## keyPress()

Send a keyboard key press to the page.

```typescript
await page.keyPress("Enter");
```

<ParamField path="key" type="string">
  The keyboard key to send.
</ParamField>

<ParamField path="options" type="PageKeyPressOptions" optional>
  Options that configure this operation.

  <ParamField path="options.delay" type="number" optional>
    The input delay in milliseconds.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## evaluate()

Evaluate JavaScript in the page and return its result.

```typescript
const title = await page.evaluate(() => document.title);
```

<ParamField path="expression" type="string | ((arg: Arg) => R | Promise<R>)">
  JavaScript source or a function to evaluate.
</ParamField>

<ParamField path="arg" type="Arg" optional>
  An optional argument passed to the function.
</ParamField>

<ResponseField name="result" type="Promise<R>">
  The operation result.
</ResponseField>

## addInitScript()

Run a script before other scripts whenever the page navigates.

```typescript
await page.addInitScript(() => {
  window.localStorage.clear();
});
```

<ParamField path="script" type="InitScriptSource<Arg>">
  A function or JavaScript source string.
</ParamField>

<ParamField path="arg" type="Arg" optional>
  An optional argument passed to the function.
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## setExtraHTTPHeaders()

Set additional HTTP headers for this page.

```typescript
await page.setExtraHTTPHeaders({ "x-test": "true" });
```

<ParamField path="headers" type="Record<string, string>">
  HTTP header names and values.
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## setViewportSize()

Set the page viewport dimensions.

```typescript
await page.setViewportSize(1440, 900);
```

<ParamField path="width" type="number">
  Viewport width in CSS pixels.
</ParamField>

<ParamField path="height" type="number">
  Viewport height in CSS pixels.
</ParamField>

<ParamField path="options" type="PageSetViewportSizeOptions" optional>
  Options that configure this operation.

  <ParamField path="options.deviceScaleFactor" type="number" optional>
    The device scale factor.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## waitForLoadState()

Wait until the page reaches a load state.

```typescript
await page.waitForLoadState("networkidle", 10_000);
```

<ParamField path="state" type="LoadState">
  The state to wait for.
</ParamField>

<ParamField path="timeout" type="number" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## waitForTimeout()

Wait for a fixed number of milliseconds.

```typescript
await page.waitForTimeout(500);
```

<ParamField path="ms" type="number">
  The number of milliseconds to wait.
</ParamField>

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## waitForSelector()

Wait for a selector to reach the requested state.

```typescript
const matched = await page.waitForSelector("main");
```

<ParamField path="selector" type="string">
  The selector to target.
</ParamField>

<ParamField path="options" type="PageWaitForSelectorOptions" optional>
  Options that configure this operation.

<ParamField path="options.pierceShadow" type="boolean" optional>
  Whether to traverse shadow roots.
</ParamField>

<ParamField path="options.state" type="string" optional>
  The state to wait for.
</ParamField>

  <ParamField path="options.timeout" type="number" optional>
    The operation timeout in milliseconds.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<boolean>">
  The operation result.
</ResponseField>

## screenshot()

Capture a screenshot of the page.

```typescript
const image = await page.screenshot({ fullPage: true });
```

<ParamField path="options" type="ScreenshotOptions" optional>
  Options that configure this operation.

<ParamField path="options.animations" type="string" optional>
  How to handle active animations.
</ParamField>

<ParamField path="options.caret" type="string" optional>
  How to render the text caret.
</ParamField>

  <ParamField path="options.clip" type="PageScreenshotClip" optional>
    The screenshot crop rectangle.

    <ParamField path="options.clip.height" type="number">
      The height in CSS pixels.
    </ParamField>

    <ParamField path="options.clip.width" type="number">
      The width in CSS pixels.
    </ParamField>

    <ParamField path="options.clip.x" type="number">
      The horizontal coordinate.
    </ParamField>

    <ParamField path="options.clip.y" type="number">
      The vertical coordinate.
    </ParamField>

  </ParamField>

<ParamField path="options.fullPage" type="boolean" optional>
  Whether to capture the full scrollable page.
</ParamField>

  <ParamField path="options.mask" type="LocatorDescriptor[]" optional>
    Elements to obscure in the screenshot.

    <ParamField path="options.mask.nth" type="number" optional>
      The zero-based match index.
    </ParamField>

    <ParamField path="options.mask.pageId" type="string">
      The owning page identifier.
    </ParamField>

    <ParamField path="options.mask.selector" type="string">
      The element selector.
    </ParamField>

  </ParamField>

<ParamField path="options.maskColor" type="string" optional>
  The CSS mask color.
</ParamField>

<ParamField path="options.omitBackground" type="boolean" optional>
  Whether to use a transparent background.
</ParamField>

<ParamField path="options.path" type="string" optional>
  A file path where the image is also written.
</ParamField>

<ParamField path="options.quality" type="number" optional>
  JPEG quality from 0 to 100.
</ParamField>

<ParamField path="options.scale" type="string" optional>
  Whether to use CSS or device pixels.
</ParamField>

<ParamField path="options.style" type="string" optional>
  CSS applied while taking the screenshot.
</ParamField>

<ParamField path="options.timeout" type="number" optional>
  The operation timeout in milliseconds.
</ParamField>

  <ParamField path="options.type" type="string" optional>
    The image format.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<Buffer>">
  The operation result.
</ResponseField>

## snapshot()

Capture the page's accessibility-oriented DOM snapshot.

```typescript
const snapshot = await page.snapshot();
```

<ParamField path="options" type="PageSnapshotOptions" optional>
  Options that configure this operation.

  <ParamField path="options.includeIframes" type="boolean" optional>
    Whether to include iframe content.
  </ParamField>
</ParamField>

<ResponseField name="result" type="Promise<SnapshotResult>">
  The operation result.

<ResponseField name="result.formattedTree" type="string">
  The formatted page tree.
</ResponseField>

<ResponseField name="result.urlMap" type="object">
  The snapshot URL lookup.
</ResponseField>

  <ResponseField name="result.xpathMap" type="object">
    The snapshot XPath lookup.
  </ResponseField>
</ResponseField>

## url()

Return the page's current URL.

```typescript
const url = await page.url();
```

<ResponseField name="result" type="Promise<string>">
  The operation result.
</ResponseField>

## title()

Return the page's current document title.

```typescript
const title = await page.title();
```

<ResponseField name="result" type="Promise<string>">
  The operation result.
</ResponseField>

## close()

Close the page.

```typescript
await page.close();
```

<ResponseField name="result" type="Promise<void>">
  Resolves after the operation completes.
</ResponseField>

## locator()

Create a locator for a CSS selector on this page.

```typescript
const locator = page.locator("button[type=submit]");
```

<ParamField path="selector" type="string">
  The selector to target.
</ParamField>

<ResponseField name="result" type="Locator">
  The operation result.
</ResponseField>

</View>

<View title="Python" icon="python">

## Quick start

```python
page = await stagehand.context.new_page()
await page.goto("https://example.com")
```

## goto()

Navigate the page to a URL.

```python
await page.goto(
    "https://example.com",
    {"wait_until": "domcontentloaded"},
)
```

<ParamField path="url" type="str">
  The destination URL.
</ParamField>

<ParamField path="options" type="PageNavigationOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.wait_until"
  type="LoadState"
  optional
>
  The load state to wait for.
</ParamField>

<ParamField path="options.timeout" type="int" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ResponseField name="result" type="Self">
  The operation result.
</ResponseField>

## reload()

Reload the current page.

```python
await page.reload()
```

<ParamField path="options" type="PageReloadOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.wait_until"
  type="LoadState"
  optional
>
  The load state to wait for.
</ParamField>

<ParamField path="options.timeout" type="int" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ParamField path="options.ignore_cache" type="bool" optional>
  Whether to bypass the browser cache.
</ParamField>

<ResponseField name="result" type="Self">
  The operation result.
</ResponseField>

## go_back()

Navigate backward in the page history.

```python
await page.go_back()
```

<ParamField path="options" type="PageNavigationOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.wait_until"
  type="LoadState"
  optional
>
  The load state to wait for.
</ParamField>

<ParamField path="options.timeout" type="int" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ResponseField name="result" type="Self">
  The operation result.
</ResponseField>

## go_forward()

Navigate forward in the page history.

```python
await page.go_forward()
```

<ParamField path="options" type="PageNavigationOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.wait_until"
  type="LoadState"
  optional
>
  The load state to wait for.
</ParamField>

<ParamField path="options.timeout" type="int" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ResponseField name="result" type="Self">
  The operation result.
</ResponseField>

## click()

Click at page coordinates.

```python
xpath = await page.click(240, 320)
```

<ParamField path="x" type="float">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="float">
  The vertical page coordinate.
</ParamField>

<ParamField path="options" type="PageClickOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.button"
  type="MouseButton"
  optional
>
  The mouse button to use.
</ParamField>

<ParamField path="options.click_count" type="int" optional>
  The number of clicks to dispatch.
</ParamField>

<ParamField path="options.return_xpath" type="bool" optional>
  Whether to return the affected element's XPath.
</ParamField>

<ResponseField name="result" type="str">
  The operation result.
</ResponseField>

## hover()

Move the pointer to page coordinates.

```python
xpath = await page.hover(240, 320)
```

<ParamField path="x" type="float">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="float">
  The vertical page coordinate.
</ParamField>

<ParamField path="options" type="PageHoverOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.return_xpath" type="bool" optional>
  Whether to return the affected element's XPath.
</ParamField>

<ResponseField name="result" type="str">
  The operation result.
</ResponseField>

## scroll()

Scroll from page coordinates by the supplied deltas.

```python
xpath = await page.scroll(240, 320, 0, 600)
```

<ParamField path="x" type="float">
  The horizontal page coordinate.
</ParamField>

<ParamField path="y" type="float">
  The vertical page coordinate.
</ParamField>

<ParamField path="delta_x" type="float">
  Horizontal scroll distance.
</ParamField>

<ParamField path="delta_y" type="float">
  Vertical scroll distance.
</ParamField>

<ParamField path="options" type="PageScrollOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.return_xpath" type="bool" optional>
  Whether to return the affected element's XPath.
</ParamField>

<ResponseField name="result" type="str">
  The operation result.
</ResponseField>

## drag_and_drop()

Drag from one page coordinate to another.

```python
from_xpath, to_xpath = await page.drag_and_drop(100, 100, 400, 400)
```

<ParamField path="from_x" type="float">
  The drag origin's horizontal coordinate.
</ParamField>

<ParamField path="from_y" type="float">
  The drag origin's vertical coordinate.
</ParamField>

<ParamField path="to_x" type="float">
  The drop target's horizontal coordinate.
</ParamField>

<ParamField path="to_y" type="float">
  The drop target's vertical coordinate.
</ParamField>

<ParamField path="options" type="PageDragAndDropOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.button"
  type="MouseButton"
  optional
>
  The mouse button to use.
</ParamField>

<ParamField path="options.steps" type="int" optional>
  The number of intermediate pointer moves.
</ParamField>

<ParamField path="options.delay" type="float" optional>
  An optional input delay in milliseconds.
</ParamField>

<ParamField path="options.return_xpath" type="bool" optional>
  Whether to return the affected element's XPath.
</ParamField>

<ResponseField name="result" type="tuple[str, str]">
  The operation result.
</ResponseField>

## type()

Type text at the current input target.

```python
await page.type("Browserbase")
```

<ParamField path="text" type="str">
  The text to type.
</ParamField>

<ParamField path="options" type="PageTypeOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.delay" type="float" optional>
  An optional input delay in milliseconds.
</ParamField>

<ParamField path="options.with_mistakes" type="bool" optional>
  Whether to simulate occasional typing mistakes.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## key_press()

Send a keyboard key press to the page.

```python
await page.key_press("Enter")
```

<ParamField path="key" type="str">
  The keyboard key to send.
</ParamField>

<ParamField path="options" type="PageKeyPressOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.delay" type="float" optional>
  An optional input delay in milliseconds.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## evaluate()

Evaluate JavaScript in the page and return its result.

```python
title = await page.evaluate("document.title", result_type=str)
```

<ParamField path="expression" type="str">
  JavaScript source or a function to evaluate.
</ParamField>

<ParamField path="result_type" type="type[EvaluateResult] | None" optional>
  An optional Python type used to validate the result.
</ParamField>

<ResponseField name="result" type="JsonValue | EvaluateResult">
  The operation result.
</ResponseField>

## add_init_script()

Run a script before other scripts whenever the page navigates.

```python
await page.add_init_script("window.localStorage.clear()")
```

<ParamField path="source" type="str | Path">
  JavaScript source or a path to a JavaScript file.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## set_extra_http_headers()

Set additional HTTP headers for this page.

```python
await page.set_extra_http_headers({"x-test": "true"})
```

<ParamField path="headers" type="Mapping[str, str]">
  HTTP header names and values.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## set_viewport_size()

Set the page viewport dimensions.

```python
await page.set_viewport_size(1440, 900)
```

<ParamField path="width" type="int">
  Viewport width in CSS pixels.
</ParamField>

<ParamField path="height" type="int">
  Viewport height in CSS pixels.
</ParamField>

<ParamField path="options" type="PageSetViewportSizeOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.device_scale_factor" type="float" optional>
  The device scale factor.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## wait_for_load_state()

Wait until the page reaches a load state.

```python
await page.wait_for_load_state("networkidle", timeout=10_000)
```

<ParamField path="state" type="LoadState | Literal['load', 'domcontentloaded', 'networkidle']">
  The state to wait for.
</ParamField>

<ParamField path="timeout" type="int | None" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## wait_for_timeout()

Wait for a fixed number of milliseconds.

```python
await page.wait_for_timeout(500)
```

<ParamField path="ms" type="int">
  The number of milliseconds to wait.
</ParamField>

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## wait_for_selector()

Wait for a selector to reach the requested state.

```python
matched = await page.wait_for_selector("main", {"state": "visible"})
```

<ParamField path="selector" type="str">
  The selector to target.
</ParamField>

<ParamField path="options" type="PageWaitForSelectorOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.state"
  type="str"
  optional
>
  The state to wait for.
</ParamField>

<ParamField path="options.timeout" type="int" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ParamField path="options.pierce_shadow" type="bool" optional>
  Whether to traverse shadow roots.
</ParamField>

<ResponseField name="result" type="bool">
  The operation result.
</ResponseField>

## screenshot()

Capture a screenshot of the page.

```python
image = await page.screenshot({"full_page": True})
```

<ParamField path="options" type="ScreenshotOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField
  path="options.animations"
  type="Literal['disabled', 'allow']"
  optional
>
  How to handle CSS animations.
</ParamField>

<ParamField path="options.caret" type="Literal['hide', 'initial']" optional>
  How to render the text caret.
</ParamField>

<ParamField path="options.clip" type="PageScreenshotClip" optional>
  The region to capture.

<ParamField path="options.clip.height" type="float">
  The height in CSS pixels.
</ParamField>

<ParamField path="options.clip.width" type="float">
  The width in CSS pixels.
</ParamField>

<ParamField path="options.clip.x" type="float">
  The horizontal coordinate.
</ParamField>

  <ParamField path="options.clip.y" type="float">
    The vertical coordinate.
  </ParamField>
</ParamField>

<ParamField path="options.full_page" type="bool" optional>
  Whether to capture the full scrollable page.
</ParamField>

<ParamField path="options.path" type="str | Path | None" optional>
  A path where the image is also written.
</ParamField>

<ParamField path="options.mask" type="Sequence[Locator] | None" optional>
  Locators to obscure in the screenshot.
</ParamField>

<ParamField path="options.mask_color" type="str" optional>
  The CSS color used for masks.
</ParamField>

<ParamField path="options.omit_background" type="bool" optional>
  Whether to use a transparent background.
</ParamField>

<ParamField path="options.quality" type="int" optional>
  JPEG quality from 0 to 100.
</ParamField>

<ParamField path="options.scale" type="Literal['css', 'device']" optional>
  Whether to use CSS or device pixel scale.
</ParamField>

<ParamField path="options.style" type="str" optional>
  CSS applied while taking the screenshot.
</ParamField>

<ParamField path="options.timeout" type="float" optional>
  Maximum wait time in milliseconds.
</ParamField>

<ParamField
  path="options.type"
  type="Literal['png', 'jpeg']"
  optional
>
  The image format.
</ParamField>

<ResponseField name="result" type="bytes">
  The operation result.
</ResponseField>

## snapshot()

Capture the page's accessibility-oriented DOM snapshot.

```python
snapshot = await page.snapshot()
```

<ParamField path="options" type="PageSnapshotOptions | None" optional>
  Options that configure this operation.
</ParamField>

<ParamField path="options.include_iframes" type="bool" optional>
  Whether to include iframe content.
</ParamField>

<ResponseField name="result" type="SnapshotResult">
  The operation result.

<ResponseField name="result.formatted_tree" type="str">
  The formatted page tree.
</ResponseField>

<ResponseField name="result.url_map" type="dict[str, object]">
  The snapshot URL lookup.
</ResponseField>

  <ResponseField name="result.xpath_map" type="dict[str, object]">
    The snapshot XPath lookup.
  </ResponseField>
</ResponseField>

## url()

Return the page's current URL.

```python
url = await page.url()
```

<ResponseField name="result" type="str">
  The operation result.
</ResponseField>

## title()

Return the page's current document title.

```python
title = await page.title()
```

<ResponseField name="result" type="str">
  The operation result.
</ResponseField>

## close()

Close the page.

```python
await page.close()
```

<ResponseField name="result" type="None">
  Resolves after the operation completes.
</ResponseField>

## locator()

Create a locator for a CSS selector on this page.

```python
locator = page.locator("button[type=submit]")
```

<ParamField path="selector" type="str">
  The selector to target.
</ParamField>

<ResponseField name="result" type="Locator">
  The operation result.
</ResponseField>

</View>
