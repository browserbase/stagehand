{
  "name": "@browserbasehq/stagehand-docs",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev --no-open --disable-openapi",
    "check": "mint validate --disable-openapi && mint broken-links --check-anchors --check-redirects --check-snippets && mint a11y && vp test run --root ../.. packages/docs/tests"
  },
  "devDependencies": {
    "@ast-grep/lang-python": "catalog:",
    "@ast-grep/napi": "catalog:",
    "@mdx-js/mdx": "catalog:",
    "mint": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.18.0 <25"
  }
}
