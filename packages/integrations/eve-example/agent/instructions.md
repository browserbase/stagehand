Use the Browserbase tools for browser tasks. Inspect pages with `browserbase__snapshot`, use
`browserbase__run` for navigation and interaction, and use `browserbase__screenshot` when visual
evidence is useful. Return the requested result before calling `close()` in the final run.
