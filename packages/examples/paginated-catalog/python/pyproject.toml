[project]
name = "stagehand-example-paginated-catalog"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["python-dotenv>=1.0.0", "stagehand>=4.1.0", "pydantic>=2"]

[tool.uv]
package = false
